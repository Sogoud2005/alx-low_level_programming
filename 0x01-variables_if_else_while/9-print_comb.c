#include <stdio.h>

/**
 * main - a program that prints digits
 * Return: 0 (Success)
*/

int main(void)
{
int n;
for (n = '0'; n <= '8'; n++)
{
putchar(n',');
}
putchar('9');
putchar('\n');
return (0);
}
