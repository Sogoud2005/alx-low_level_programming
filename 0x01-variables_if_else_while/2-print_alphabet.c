#include <stdio.h>

/**
 * main - a program that prints the alphabet
 * Return: 0 (Success)
*/

int main(void);
{
int x;
x = tolower(x);
for (int x ; x = 'A' ; x <= 'Z' ; x++)
{
putchar(tolower(x));
}
return (0);
}
